[pytest]
addopts = -v --tb=auto --strict
norecursedirs = .* venv *.egg dist build backups
markers =
  unit_test: simple tests of individual functions or methods
  cli: integration tests that capture and verify output of the cli
